export const metadata = {
  title: "Generate Bill | PharmaTrack",
  description: "Bill generate page of PharmaTrack",
};

export default function BillGenerateLayout({ children }) {
  return <>{children}</>;
}
