export const metadata = {
  title: "Add Product | PharmaTrack",
  description: "Add product page of PharmaTrack",
};

export default function AddProductLayout({ children }) {
  return <>{children}</>;
}
